/** Authentication page — QR scan + face verification. */

function AuthPage() {
  return (
    <div className="page auth-page">
      <h1>FortisExam</h1>
      <p>Scan your QR code to begin</p>
      {/* TODO: QR scanner component (webcam), face capture, auth flow */}
    </div>
  );
}

export default AuthPage;
